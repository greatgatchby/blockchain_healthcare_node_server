const create = (req,res) => {
    res.status(200).send('Testing endpoint')
}
const get = (req,res) => {
    res.status(200).send('Testing endpoint')
}
const getById = (req,res) => {
    res.status(200).send('Testing endpoint')
}
const update = (req,res) => {
    res.status(200).send('Testing endpoint')
}
const redact = (req,res) => {
    res.status(200).send('Testing endpoint')
}
module.exports = {
    create,
    get,
    getById,
    update,
    redact
}
